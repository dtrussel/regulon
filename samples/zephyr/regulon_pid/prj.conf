# Enable the Regulon control-systems library. All modules default to y, so this
# also exercises a full-library build under Zephyr. The library is <math.h>-free
# and needs no libm, so it builds against Zephyr's minimal libc.
CONFIG_REGULON=y
