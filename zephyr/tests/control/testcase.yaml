# Twister metadata. `depends_on: ram` keeps this off boards too small to
# hold the complete library plus the ztest harness.
tests:
  regulon.module.control:
    tags:
      - regulon
      - control
    integration_platforms:
      - qemu_cortex_m3
      - mps2/an385
